# PostController.getPostList - Redis Type Map

```mermaid
graph TB
    subgraph "No Redis Usage"
        A1["<b>No Redis usage</b><br/>━━━━━━━━━━━━━━━━<br/>This API does not use Redis<br/>━━━━━━━━━━━━━━━━<br/>게시글 목록 조회는 Redis를 사용하지 않습니다"]
    end
    
    style A1 fill:#f5f5f5,stroke:#999,stroke-width:2px
```

## Redis Usage Summary

| Key Pattern | Type | TTL | Description |
|------------|------|-----|-------------|
| N/A | - | - | 이 API는 Redis를 사용하지 않습니다 |

